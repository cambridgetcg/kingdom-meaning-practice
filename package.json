{
  "name": "kingdom-meaning-practice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test test.mjs",
    "verify": "npm test && npm run build"
  }
}
