# Resting Cloudflare baseline

This static bundle is the deliberate rollback state for the
`kingdom-folding-feedback` Cloudflare Pages project. The served page makes no
scientific claim, carries no source digest, accepts no input, and starts no
action.

Deploy it from an exact clean commit and keep the resulting deployment ID as a
known resting rollback target.
